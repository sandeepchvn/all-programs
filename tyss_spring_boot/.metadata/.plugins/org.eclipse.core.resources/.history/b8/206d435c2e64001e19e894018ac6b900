package com.ty.swaggerdoc.contoller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RestController;

import com.ty.swaggerdoc.entity.User;
import com.ty.swaggerdoc.repository.UserRepository;

import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.parameters.RequestBody;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.responses.ApiResponses;

@RestController
public class UserController {

	@Autowired
	private UserRepository repository;

	@Operation(description = "To save the user details ", summary ="To save the user details into the DB")
	@ApiResponses(value=@ApiResponse(description = "usersaved",responseCode = "201"))
	@PostMapping("/save")
	public String saveUser(@RequestBody User user) {
		repository.save(user);
		System.out.println(user.getName());
		return "user is saved";
	}
}
